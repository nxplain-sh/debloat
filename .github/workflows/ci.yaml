name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  go:
    name: Build & test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: go vet
        run: go vet ./...

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v8
        with:
          # renovate: datasource=github-releases depName=golangci/golangci-lint
          version: v2.13.2

      - name: Test
        run: go test -race ./...

      - name: Build
        run: go build ./...

  docs:
    name: Docs & config format
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Prettier
        run: npm ci

      - name: Prettier (check formatting)
        run: npx --yes prettier --check . --ignore-unknown
